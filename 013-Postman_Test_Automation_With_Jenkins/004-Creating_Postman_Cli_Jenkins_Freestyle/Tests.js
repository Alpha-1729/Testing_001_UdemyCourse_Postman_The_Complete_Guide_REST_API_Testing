/*
 * Build Steps → Execute Shell → Add the following commands:
 *   postman --version
 *   postman login --with-api-key 'api-key'
 *   postman collection run 'collection_id'
 *
 * ⚠️ Always wrap api-key and collection_id in single quotes.
 *    Commands are copied directly from Postman CLI.
 *
 * Click on Build Now → view details in the Console Output.
 * Check test status under the Runs section in Postman.
 */