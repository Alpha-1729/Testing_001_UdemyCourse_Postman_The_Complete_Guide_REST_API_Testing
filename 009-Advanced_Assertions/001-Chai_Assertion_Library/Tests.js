/*
 * Chai — an assertion library built into Postman for writing human-readable tests.
 * Uses BDD (Behavior Driven Development) style syntax — to, be, have, include, equal, etc.
 * Reference: https://www.chaijs.com/api/bdd/
 *
 * Common Chai assertions used in Postman.
 *   pm.expect(value).to.eql("expected")       — deep equality check
 *   pm.expect(value).to.be.a("string")        — type check
 *   pm.expect(value).to.be.true               — strictly boolean true
 *   pm.expect(value).to.include("substring")  — substring or array inclusion
 *   pm.expect(value).to.have.property("key")  — object has a property
 *   pm.expect(value).to.be.above(0)           — number greater than 0
 *   pm.expect(value).to.be.null               — value is null
 *   pm.expect(array).to.have.lengthOf(3)      — array length check
 */
