/*
 * Running a collection with Newman.
 *   newman run postman_collection_url
 *
 * How to get the collection_url:
 *   1. Click on the collection → Click on Share → Click on Via API → Copy the URL.
 *   2. Update all initial values with the current values before running.
 *
 * Verify the collection URL in Postman:
 *   GET https://api.postman.com/collections/[collection_id]/?access_key=[api_key]
 *
 *   New command:
 *   GET https://api.postman.com/collections/[collection_id]/?apikey=[api_key]
 *   Run this as a request in Postman to get all collection details.
 *
 * Get your Postman API key:
 *   Reference Link : https://web.postman.co/settings/me/api-keys
 */
