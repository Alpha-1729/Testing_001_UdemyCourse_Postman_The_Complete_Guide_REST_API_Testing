# First Postman Script

- **Pre-request Script** — code that executes before the request is sent.
  - Open the `Console` to view the logs.
- **Scripts tab** — code that executes after the request is sent.
- In Postman, only JavaScript can be used to write scripts and tests.
