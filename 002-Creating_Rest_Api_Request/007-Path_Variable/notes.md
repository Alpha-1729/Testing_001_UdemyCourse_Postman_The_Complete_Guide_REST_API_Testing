# Path Variable

- Request with path variable.
  - `{{baseUrl}}/products/:productId`
  - Postman will automatically create a **Path Variables** section.
  - Add a value to `productId` under the Path Variables section.
  - Refer the `assets/` folder for screenshots.
