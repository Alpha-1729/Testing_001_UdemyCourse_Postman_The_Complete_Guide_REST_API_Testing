# HEAD Request Method

- `HEAD` — used to validate an endpoint without fetching the response body.
- Validate an endpoint.
  - `HEAD` `{{baseUrl}}/products`
- Lightweight — no database operation is performed.
- Use cases.
  - Check whether the API is up or not.
  - Check whether the endpoint is valid.
  - Check whether the authorization is correct.
